<UserControl x:Class="FineTekDeivePlatform.View.LedDigitalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FineTekDeivePlatform.View"
             xmlns:dvd="clr-namespace:LedDigital.Controls;assembly=LedDigital"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!--   <dvd:LedDigital x:Name="singleDigital" Value="{Binding SingleDigitalValue}" DigitalDimBrush="Blue" DigitalBrush="OrangeRed" Height="60" Width="30" HorizontalAlignment="Left"/>-->
        <dvd:LedDigitalPanel x:Name="multiDigital"  DigitalCount ="10" Margin="0" Height="30" Width="150
                             " HorizontalAlignment="Left"
                             Value="{Binding MultiDigitalValue}" DigitalBrush="{Binding MultiDigitalBrush}" Background="{Binding MultiDigitalBackgroundBrush}" DigitalThickness="2" BevelWidth="0.5"/>

    </Grid>
</UserControl>
